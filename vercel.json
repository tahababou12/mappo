{
  "version": 2,
  "buildCommand": "cd backend && pnpm install --no-frozen-lockfile && pnpm build && cd ../frontend && pnpm install --no-frozen-lockfile && pnpm build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "functions": {
    "api/**/*.js": {
      "runtime": "@vercel/node@18.x",
      "memory": 1024
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
} 